package algorithm_lesson.chapter04;

@SuppressWarnings({"all"})
//最优装载
public class BestLoading {
//    void Loading(int x[],  Type w[], Type c, int n)
//    {
//        Sort(w,n); //按重量由轻到重排序
//        for (int i = 1; i <= n; i++) x[i] = 0;
//        for (int i = 1; i <= n && w[t[i]] <= c; i++) {x[i] = 1; c -= w[i];}
//    }
}
